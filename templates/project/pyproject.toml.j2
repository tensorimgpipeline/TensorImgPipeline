[project]
name = "{{ project_name }}"
version = "0.1.0"
description = "{{ description }}"
requires-python = ">=3.9"
dependencies = [
    "tensorimgpipeline",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
