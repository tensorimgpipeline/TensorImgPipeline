# Load data process - loads data from disk
[processes.load_data]
type = "LoadDataProcess"
force = false

# Process data process - processes the loaded data
[processes.process_data]
type = "ProcessDataProcess"
force = false
